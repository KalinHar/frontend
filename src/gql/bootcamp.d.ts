export type BootcampersType = {
  id: UUID
  firstName: string
  lastName: string
}
export type BootcampersResponse = {
  id: UUID
  firstName: string
  lastName: string
}

export type BootcamperInput = {
  firstName: string
  lastName: string
}
